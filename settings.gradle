pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        maven {
            url 'https://nexus.inductiveautomation.com/repository/public'
        }
    }
}

rootProject.name = "process-config-manager"

include(
":",
    ":common",
    ":gateway",
    ":client",
    ":designer"
)
